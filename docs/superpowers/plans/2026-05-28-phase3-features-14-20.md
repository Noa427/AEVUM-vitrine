# Phase 3 Vitrine — Features 14, 15, 16, 17, 19, 20 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter 3 nouveaux templates d'email automatisés, les toggles WhatsApp/SMS par template, la connexion WhatsApp dans settings, le rapport vidéo hebdomadaire, et le badge churn dans le drawer élèves.

**Architecture:** Additions inline directes dans les fichiers existants sans extraction de composants. `parseConfig` étendu avec `whatsapp_active`, `sms_active`, `sms_body`. Chaque section dans customize.astro suit le pattern `cust-card config-panel` existant. `initEditor()` enrichi pour les nouveaux toggles.

**Tech Stack:** Astro 6, TypeScript strict, SSR (`prerender = false`), fetch vers backend `AEVUM_URL`, CSS pur (design B&W premium)

---

## File Map

| Fichier | Action | Raison |
|---------|--------|--------|
| `src/pages/client/customize.astro` | Modify | F14, F15, F16, F19, F20 — nouveaux panels + toggles WA/SMS |
| `src/pages/client/settings.astro` | Modify | F16 WA section, F17 rapport vidéo, F20 SMS section |
| `src/pages/client/students.astro` | Modify | F15 — badge churn dans drawer |
| `src/pages/client/history.astro` | Modify | F17 — lien vidéo pour `rapport_video_sent` |
| `src/pages/api/whatsapp-connect.ts` | Create | F16 — proxy POST backend |
| `src/pages/api/whatsapp-disconnect.ts` | Create | F16 — proxy DELETE backend |

---

## Task 1: Frontmatter customize.astro — parseConfig + VARIABLE_HINTS + /client/me

**Files:**
- Modify: `src/pages/client/customize.astro` (lignes 87–206)

- [ ] **Step 1: Étendre `VARIABLE_HINTS` (ligne 127–138)**

Remplacer le bloc `VARIABLE_HINTS` entier par :
```ts
const VARIABLE_HINTS: Record<string, string[]> = {
  template_onboarding_j0: ['{{nom}}', '{{prenom}}', '{{email}}', '{{nom_formation}}', '{{lien_acces}}', '{{mot_de_passe}}'],
  template_onboarding_j3: ['{{nom}}', '{{prenom}}', '{{email}}', '{{nom_formation}}', '{{lien_acces}}'],
  template_onboarding_j7: ['{{nom}}', '{{prenom}}', '{{email}}', '{{nom_formation}}', '{{lien_acces}}'],
  template_failed_payment_j1: ['{{nom}}', '{{prenom}}', '{{email}}', '{{montant}}', '{{lien_paiement}}'],
  template_failed_payment_j3: ['{{nom}}', '{{prenom}}', '{{email}}', '{{montant}}', '{{lien_paiement}}'],
  template_failed_payment_j7: ['{{nom}}', '{{prenom}}', '{{email}}', '{{montant}}', '{{lien_paiement}}'],
  template_checkout_abandon: ['{{nom}}', '{{prenom}}', '{{email}}', '{{nom_formation}}', '{{lien_checkout}}'],
  testimonial_url: [],
  template_testimonial_j30: ['{{nom}}', '{{prenom}}', '{{nom_formation}}', '{{lien_temoignage}}'],
  template_testimonial_j60: ['{{nom}}', '{{prenom}}', '{{nom_formation}}', '{{lien_temoignage}}'],
  template_predunning: ['{{nom}}', '{{prenom}}', '{{email}}', '{{nom_formation}}', '{{lien_paiement}}', '{{date_expiration}}'],
  template_churn_reengagement: ['{{nom}}', '{{prenom}}', '{{nom_formation}}', '{{lien_acces}}'],
  template_coaching_j14: ['{{nom}}', '{{prenom}}', '{{nom_formation}}', '{{lien_acces}}'],
};
```

- [ ] **Step 2: Étendre `parseConfig` (ligne 154–162)**

Remplacer la fonction `parseConfig` par :
```ts
function parseConfig(raw: string): {
  subject: string; body: string; label?: string; active: boolean;
  whatsapp_active: boolean; sms_active: boolean; sms_body: string;
} {
  try {
    const p = JSON.parse(raw);
    if (p && typeof p.subject === 'string' && typeof p.body === 'string') {
      return {
        subject: p.subject,
        body: p.body,
        label: typeof p.label === 'string' && p.label ? p.label : undefined,
        active: p.active !== false,
        whatsapp_active: p.whatsapp_active === true,
        sms_active: p.sms_active === true,
        sms_body: typeof p.sms_body === 'string' ? p.sms_body : '',
      };
    }
  } catch {}
  return { subject: '', body: raw, active: true, whatsapp_active: false, sms_active: false, sms_body: '' };
}
```

- [ ] **Step 3: Ajouter `/client/me` au `Promise.allSettled` (ligne 87–92)**

Remplacer le bloc `Promise.allSettled` et le filtre `fulfilled` par :
```ts
const [configsResult, customAutoResult, meResult] = await Promise.allSettled([
  fetch(`${base}/client/configs`, { headers, signal: AbortSignal.timeout(8000) }),
  fetch(`${base}/client/automations/custom`, { headers, signal: AbortSignal.timeout(8000) }),
  fetch(`${base}/client/me`, { headers, signal: AbortSignal.timeout(8000) }),
]);

const fulfilled = [configsResult, customAutoResult, meResult].filter((r): r is PromiseFulfilledResult<Response> => r.status === 'fulfilled');
if (fulfilled.some((r) => r.value.status === 401)) {
  Astro.cookies.delete('aevum_token', { path: '/' });
  return Astro.redirect('/login');
}
```

- [ ] **Step 4: Extraire `whatsapp_connected` depuis `meResult` (après ligne 125, avant `VARIABLE_HINTS`)**

Ajouter après le bloc `customAutomations` (ligne 125) :
```ts
let whatsappConnected = false;
if (meResult.status === 'fulfilled' && meResult.value.ok) {
  const meData = await meResult.value.json().catch(() => ({})) as { whatsapp_connected?: boolean };
  whatsappConnected = meData.whatsapp_connected === true;
}
```

- [ ] **Step 5: Mettre à jour le type de `PARSED_CONFIGS` (ligne 193–196)**

Remplacer :
```ts
const PARSED_CONFIGS: Record<string, { subject: string; body: string; label?: string; active: boolean }> = {};
for (const { key } of EMAIL_CONFIGS) {
  PARSED_CONFIGS[key] = parseConfig(configs[key] ?? '');
}
```
Par :
```ts
const PARSED_CONFIGS: Record<string, ReturnType<typeof parseConfig>> = {};
for (const { key } of EMAIL_CONFIGS) {
  PARSED_CONFIGS[key] = parseConfig(configs[key] ?? '');
}
```

- [ ] **Step 6: Ajouter les 3 nouvelles configs (après ligne 205, avant `---`)**

Ajouter après `const testimonialJ60Config = parseConfig(testimonialJ60Raw ?? '');` :
```ts
const predunningConfig = parseConfig(configs['template_predunning'] ?? '');
const churnConfig = parseConfig(configs['template_churn_reengagement'] ?? '');
const coachingConfig = parseConfig(configs['template_coaching_j14'] ?? '');
```

- [ ] **Step 7: Vérifier le build TypeScript**

```bash
npm run build
```
Attendu : `0 errors`. Si des erreurs TypeScript sur les nouveaux champs, vérifier que `parseConfig` retourne bien tous les champs.

- [ ] **Step 8: Commit**

```bash
git add src/pages/client/customize.astro
git commit -m "feat(customize): extend parseConfig + VARIABLE_HINTS + /client/me for Phase 3"
```

---

## Task 2: EMAIL_CONFIGS loop panel HTML — data attrs + toggles WA/SMS

**Files:**
- Modify: `src/pages/client/customize.astro` (section du loop JSX, lignes 265–388)

Le but : dans le `EMAIL_CONFIGS.map()`, ajouter les data attributes WA/SMS sur le `<div>` panel, les toggles WA et SMS dans `.panel-right-actions`, et le bloc `.sms-editor` dans le formulaire manuel.

- [ ] **Step 1: Ajouter data attrs sur le panel `<div>` (dans le map, ligne ~272)**

Trouver la ligne qui ouvre le div panel dans le map :
```astro
<div class="cust-card config-panel" id={`panel-${key}`} data-config={key}
     data-subject={parsed.subject} data-body={parsed.body}
     data-label={parsed.label ?? ''} data-panel-active={parsed.active ? 'true' : 'false'}
     style={!isActive ? "display:none" : ""}>
```
La remplacer par :
```astro
<div class="cust-card config-panel" id={`panel-${key}`} data-config={key}
     data-subject={parsed.subject} data-body={parsed.body}
     data-label={parsed.label ?? ''} data-panel-active={parsed.active ? 'true' : 'false'}
     data-whatsapp-active={parsed.whatsapp_active ? 'true' : 'false'}
     data-sms-active={parsed.sms_active ? 'true' : 'false'}
     data-sms-body={parsed.sms_body ?? ''}
     style={!isActive ? "display:none" : ""}>
```

- [ ] **Step 2: Ajouter les toggles WA + SMS dans `.panel-right-actions` (dans le map)**

Dans le map, trouver le bloc `.panel-right-actions` :
```astro
<div class="panel-right-actions">
  <label class="toggle-switch" title="Activer/désactiver cet email">
    <input type="checkbox" class="toggle-input" checked={parsed.active} />
    <span class="toggle-track"></span>
    <span class="toggle-label">{parsed.active ? 'Actif' : 'Inactif'}</span>
  </label>
  <form method="POST" class="reset-form">
```
Remplacer par :
```astro
<div class="panel-right-actions">
  <label class="toggle-switch" title="Activer/désactiver cet email">
    <input type="checkbox" class="toggle-input" checked={parsed.active} />
    <span class="toggle-track"></span>
    <span class="toggle-label">{parsed.active ? 'Actif' : 'Inactif'}</span>
  </label>
  {whatsappConnected && (
    <label class="toggle-switch toggle-wa" title="Envoyer aussi par WhatsApp">
      <input type="checkbox" class="toggle-wa-input" checked={parsed.whatsapp_active} />
      <span class="toggle-track"></span>
      <span class="toggle-label-wa">WhatsApp</span>
    </label>
  )}
  <label class="toggle-switch toggle-sms" title="Envoyer aussi par SMS">
    <input type="checkbox" class="toggle-sms-input" checked={parsed.sms_active} />
    <span class="toggle-track"></span>
    <span class="toggle-label-sms">SMS</span>
  </label>
  <form method="POST" class="reset-form">
```

- [ ] **Step 3: Ajouter le bloc `.sms-editor` dans le formulaire manuel (dans le map)**

Dans le map, trouver dans le `<div class="tab-panel" data-panel="manual">`, après le bloc `<div class="cust-hints">...</div>` et avant `<div class="manual-actions">` :

Insérer :
```astro
<div class="sms-editor" style="display:none">
  <label class="form-label" style="margin-top:0.75rem">Corps du SMS</label>
  <textarea class="form-input sms-body-input" rows={3} maxlength={160} disabled
            placeholder="Ex : Bonjour {{nom}}, votre accès est disponible. Lien : {{lien_acces}}"></textarea>
  <div class="sms-counter"><span class="sms-char-count">0</span>/160</div>
  <p class="sms-note">160 caractères max. Les variables sont remplacées par les vraies données.</p>
</div>
```

- [ ] **Step 4: Vérifier le build**

```bash
npm run build
```
Attendu : `0 errors`. Si erreur sur `parsed.whatsapp_active`, vérifier Task 1 Step 5 (type PARSED_CONFIGS).

- [ ] **Step 5: Commit**

```bash
git add src/pages/client/customize.astro
git commit -m "feat(customize): add WA/SMS toggles and sms-editor to email tab panels"
```

---

## Task 3: Insérer les 3 nouveaux panels F14, F15, F19 (customize.astro)

**Files:**
- Modify: `src/pages/client/customize.astro` (après la fermeture `.config-panel-wrap`, avant `<!-- F11 — Checkout abandon -->`)

Les 3 panels (predunning, churn, coaching) suivent exactement le pattern du panel `checkout_abandon`. Chacun a : section-divider, header avec toggles, note explicative, éditeur Manuel + IA, test-send.

- [ ] **Step 1: Insérer le panel F14 (template_predunning)**

Trouver la ligne `<!-- F11 — Checkout abandon -->` et insérer AVANT :
```astro
<!-- F14 — Pré-dunning -->
<div class="section-divider"><span>Pré-dunning (CB expirant bientôt)</span></div>

<div class="cust-card config-panel"
     id="panel-template_predunning"
     data-config="template_predunning"
     data-subject={predunningConfig.subject}
     data-body={predunningConfig.body}
     data-panel-active={predunningConfig.active ? 'true' : 'false'}
     data-whatsapp-active={predunningConfig.whatsapp_active ? 'true' : 'false'}
     data-sms-active={predunningConfig.sms_active ? 'true' : 'false'}
     data-sms-body={predunningConfig.sms_body ?? ''}>

  <div class="panel-header">
    <div class="panel-title-group">
      <h2 class="cust-card-title section-title" style="margin-bottom:0">Email pré-dunning</h2>
    </div>
    <div class="panel-right-actions">
      <label class="toggle-switch" title="Activer/désactiver">
        <input type="checkbox" class="toggle-input" checked={predunningConfig.active} />
        <span class="toggle-track"></span>
        <span class="toggle-label">{predunningConfig.active ? 'Actif' : 'Inactif'}</span>
      </label>
      {whatsappConnected && (
        <label class="toggle-switch toggle-wa" title="Envoyer aussi par WhatsApp">
          <input type="checkbox" class="toggle-wa-input" checked={predunningConfig.whatsapp_active} />
          <span class="toggle-track"></span>
          <span class="toggle-label-wa">WhatsApp</span>
        </label>
      )}
      <label class="toggle-switch toggle-sms" title="Envoyer aussi par SMS">
        <input type="checkbox" class="toggle-sms-input" checked={predunningConfig.sms_active} />
        <span class="toggle-track"></span>
        <span class="toggle-label-sms">SMS</span>
      </label>
    </div>
  </div>

  <p class="card-note" style="margin:0 0 1rem;font-size:0.875rem;color:var(--fg-muted)">
    Envoyé automatiquement quand une carte bancaire expire dans moins de 14 jours.
  </p>

  {successType === 'template_predunning' && <div class="success-msg" role="alert"><span>Sauvegardé.</span></div>}
  {errorType === 'template_predunning' && <div class="cust-field-error" role="alert">{errorMsg}</div>}

  <div class="editor-tabs-row">
    <div class="editor-tabs">
      <button class="editor-tab active" data-tab="manual">✏️ Rédiger moi-même</button>
      <button class="editor-tab" data-tab="generate">✨ Générer avec l'IA</button>
    </div>
  </div>

  <div class="tab-panel" data-panel="manual">
    <form method="POST" class="cust-form">
      <input type="hidden" name="config_type" value="template_predunning" />
      <input type="hidden" name="value" class="hidden-value" value={JSON.stringify(predunningConfig)} />
      <label class="form-label">Sujet</label>
      <input type="text" class="form-input subject-input" placeholder="Sujet de l'email..." />
      <label class="form-label" style="margin-top:0.25rem">Corps</label>
      <textarea class="form-input cust-textarea body-input" rows={7} placeholder="Contenu de l'email..."></textarea>
      <div class="cust-hints">
        <p class="cust-hints-label">Variables disponibles — cliquez pour insérer :</p>
        <div class="cust-hints-list">
          {VARIABLE_HINTS['template_predunning']!.map((v) => (
            <code class="cust-hint" data-var={v}>{v}</code>
          ))}
        </div>
      </div>
      <div class="sms-editor" style="display:none">
        <label class="form-label" style="margin-top:0.75rem">Corps du SMS</label>
        <textarea class="form-input sms-body-input" rows={3} maxlength={160} disabled
                  placeholder="Ex : Bonjour {{nom}}, votre CB expire bientôt. Mettez à jour vos infos : {{lien_paiement}}"></textarea>
        <div class="sms-counter"><span class="sms-char-count">0</span>/160</div>
        <p class="sms-note">160 caractères max. Les variables sont remplacées par les vraies données.</p>
      </div>
      <div class="manual-actions">
        <button type="submit" class="btn btn-primary cust-save-btn">Sauvegarder</button>
        <button type="button" class="btn btn-secondary btn-improve">🔧 Améliorer avec l'IA</button>
      </div>
      <p class="ai-error improve-error"></p>
    </form>
    <div class="test-send-wrap">
      <button type="button" class="btn btn-secondary btn-test-send"
              data-config-type="template_predunning" data-email={auth.email}>
        📧 M'envoyer un aperçu
      </button>
      <span class="test-send-feedback" aria-live="polite"></span>
    </div>
  </div>

  <div class="tab-panel hidden" data-panel="generate">
    <input type="text" class="form-input gen-formation" placeholder="Nom de votre formation" />
    <select class="form-input gen-tone">
      <option value="chaleureux">Chaleureux et bienveillant</option>
      <option value="professionnel">Professionnel et direct</option>
      <option value="motivant">Motivant et dynamique</option>
    </select>
    <input type="text" class="form-input gen-objective" placeholder="Objectif (ex : alerter avant expiration CB)" />
    <button class="btn btn-primary btn-generate" style="align-self:flex-start">✨ Générer l'email</button>
    <p class="ai-error gen-error"></p>
    <div class="generation-result hidden">
      <input type="text" class="form-input subject-preview" readonly placeholder="Sujet généré" />
      <textarea class="form-input body-preview" rows={6} readonly placeholder="Contenu généré"></textarea>
      <button class="btn btn-secondary btn-use-result" style="align-self:flex-start">Utiliser ce contenu →</button>
    </div>
  </div>
</div>
```

- [ ] **Step 2: Insérer le panel F15 (template_churn_reengagement)**

Insérer juste après le panel F14, avant `<!-- F11 — Checkout abandon -->` :
```astro
<!-- F15 — Re-engagement churn -->
<div class="section-divider"><span>Re-engagement (élèves inactifs)</span></div>

<div class="cust-card config-panel"
     id="panel-template_churn_reengagement"
     data-config="template_churn_reengagement"
     data-subject={churnConfig.subject}
     data-body={churnConfig.body}
     data-panel-active={churnConfig.active ? 'true' : 'false'}
     data-whatsapp-active={churnConfig.whatsapp_active ? 'true' : 'false'}
     data-sms-active={churnConfig.sms_active ? 'true' : 'false'}
     data-sms-body={churnConfig.sms_body ?? ''}>

  <div class="panel-header">
    <div class="panel-title-group">
      <h2 class="cust-card-title section-title" style="margin-bottom:0">Email re-engagement</h2>
    </div>
    <div class="panel-right-actions">
      <label class="toggle-switch" title="Activer/désactiver">
        <input type="checkbox" class="toggle-input" checked={churnConfig.active} />
        <span class="toggle-track"></span>
        <span class="toggle-label">{churnConfig.active ? 'Actif' : 'Inactif'}</span>
      </label>
      {whatsappConnected && (
        <label class="toggle-switch toggle-wa" title="Envoyer aussi par WhatsApp">
          <input type="checkbox" class="toggle-wa-input" checked={churnConfig.whatsapp_active} />
          <span class="toggle-track"></span>
          <span class="toggle-label-wa">WhatsApp</span>
        </label>
      )}
      <label class="toggle-switch toggle-sms" title="Envoyer aussi par SMS">
        <input type="checkbox" class="toggle-sms-input" checked={churnConfig.sms_active} />
        <span class="toggle-track"></span>
        <span class="toggle-label-sms">SMS</span>
      </label>
    </div>
  </div>

  <p class="card-note" style="margin:0 0 1rem;font-size:0.875rem;color:var(--fg-muted)">
    Envoyé aux élèves n'ayant ouvert aucun email depuis 21 jours. Déclenché une seule fois par tranche de 30 jours.
  </p>

  {successType === 'template_churn_reengagement' && <div class="success-msg" role="alert"><span>Sauvegardé.</span></div>}
  {errorType === 'template_churn_reengagement' && <div class="cust-field-error" role="alert">{errorMsg}</div>}

  <div class="editor-tabs-row">
    <div class="editor-tabs">
      <button class="editor-tab active" data-tab="manual">✏️ Rédiger moi-même</button>
      <button class="editor-tab" data-tab="generate">✨ Générer avec l'IA</button>
    </div>
  </div>

  <div class="tab-panel" data-panel="manual">
    <form method="POST" class="cust-form">
      <input type="hidden" name="config_type" value="template_churn_reengagement" />
      <input type="hidden" name="value" class="hidden-value" value={JSON.stringify(churnConfig)} />
      <label class="form-label">Sujet</label>
      <input type="text" class="form-input subject-input" placeholder="Sujet de l'email..." />
      <label class="form-label" style="margin-top:0.25rem">Corps</label>
      <textarea class="form-input cust-textarea body-input" rows={7} placeholder="Contenu de l'email..."></textarea>
      <div class="cust-hints">
        <p class="cust-hints-label">Variables disponibles — cliquez pour insérer :</p>
        <div class="cust-hints-list">
          {VARIABLE_HINTS['template_churn_reengagement']!.map((v) => (
            <code class="cust-hint" data-var={v}>{v}</code>
          ))}
        </div>
      </div>
      <div class="sms-editor" style="display:none">
        <label class="form-label" style="margin-top:0.75rem">Corps du SMS</label>
        <textarea class="form-input sms-body-input" rows={3} maxlength={160} disabled
                  placeholder="Ex : Bonjour {{nom}}, vous nous manquez dans {{nom_formation}}. Reprenez ici : {{lien_acces}}"></textarea>
        <div class="sms-counter"><span class="sms-char-count">0</span>/160</div>
        <p class="sms-note">160 caractères max. Les variables sont remplacées par les vraies données.</p>
      </div>
      <div class="manual-actions">
        <button type="submit" class="btn btn-primary cust-save-btn">Sauvegarder</button>
        <button type="button" class="btn btn-secondary btn-improve">🔧 Améliorer avec l'IA</button>
      </div>
      <p class="ai-error improve-error"></p>
    </form>
    <div class="test-send-wrap">
      <button type="button" class="btn btn-secondary btn-test-send"
              data-config-type="template_churn_reengagement" data-email={auth.email}>
        📧 M'envoyer un aperçu
      </button>
      <span class="test-send-feedback" aria-live="polite"></span>
    </div>
  </div>

  <div class="tab-panel hidden" data-panel="generate">
    <input type="text" class="form-input gen-formation" placeholder="Nom de votre formation" />
    <select class="form-input gen-tone">
      <option value="chaleureux">Chaleureux et bienveillant</option>
      <option value="professionnel">Professionnel et direct</option>
      <option value="motivant">Motivant et dynamique</option>
    </select>
    <input type="text" class="form-input gen-objective" placeholder="Objectif (ex : réactiver des élèves inactifs)" />
    <button class="btn btn-primary btn-generate" style="align-self:flex-start">✨ Générer l'email</button>
    <p class="ai-error gen-error"></p>
    <div class="generation-result hidden">
      <input type="text" class="form-input subject-preview" readonly placeholder="Sujet généré" />
      <textarea class="form-input body-preview" rows={6} readonly placeholder="Contenu généré"></textarea>
      <button class="btn btn-secondary btn-use-result" style="align-self:flex-start">Utiliser ce contenu →</button>
    </div>
  </div>
</div>
```

- [ ] **Step 3: Insérer le panel F19 (template_coaching_j14)**

Insérer juste après le panel F15, avant `<!-- F11 — Checkout abandon -->` :
```astro
<!-- F19 — Coaching élèves inactifs -->
<div class="section-divider"><span>Coaching élèves inactifs</span></div>

<div class="cust-card config-panel"
     id="panel-template_coaching_j14"
     data-config="template_coaching_j14"
     data-subject={coachingConfig.subject}
     data-body={coachingConfig.body}
     data-panel-active={coachingConfig.active ? 'true' : 'false'}
     data-whatsapp-active={coachingConfig.whatsapp_active ? 'true' : 'false'}
     data-sms-active={coachingConfig.sms_active ? 'true' : 'false'}
     data-sms-body={coachingConfig.sms_body ?? ''}>

  <div class="panel-header">
    <div class="panel-title-group">
      <h2 class="cust-card-title section-title" style="margin-bottom:0">Email coaching</h2>
    </div>
    <div class="panel-right-actions">
      <label class="toggle-switch" title="Activer/désactiver">
        <input type="checkbox" class="toggle-input" checked={coachingConfig.active} />
        <span class="toggle-track"></span>
        <span class="toggle-label">{coachingConfig.active ? 'Actif' : 'Inactif'}</span>
      </label>
      {whatsappConnected && (
        <label class="toggle-switch toggle-wa" title="Envoyer aussi par WhatsApp">
          <input type="checkbox" class="toggle-wa-input" checked={coachingConfig.whatsapp_active} />
          <span class="toggle-track"></span>
          <span class="toggle-label-wa">WhatsApp</span>
        </label>
      )}
      <label class="toggle-switch toggle-sms" title="Envoyer aussi par SMS">
        <input type="checkbox" class="toggle-sms-input" checked={coachingConfig.sms_active} />
        <span class="toggle-track"></span>
        <span class="toggle-label-sms">SMS</span>
      </label>
    </div>
  </div>

  <p class="card-note" style="margin:0 0 1rem;font-size:0.875rem;color:var(--fg-muted)">
    Envoyé aux élèves n'ayant pas consulté leurs emails depuis 14 jours. Différent du re-engagement : cible les élèves qui n'ont pas encore commencé.
  </p>

  {successType === 'template_coaching_j14' && <div class="success-msg" role="alert"><span>Sauvegardé.</span></div>}
  {errorType === 'template_coaching_j14' && <div class="cust-field-error" role="alert">{errorMsg}</div>}

  <div class="editor-tabs-row">
    <div class="editor-tabs">
      <button class="editor-tab active" data-tab="manual">✏️ Rédiger moi-même</button>
      <button class="editor-tab" data-tab="generate">✨ Générer avec l'IA</button>
    </div>
  </div>

  <div class="tab-panel" data-panel="manual">
    <form method="POST" class="cust-form">
      <input type="hidden" name="config_type" value="template_coaching_j14" />
      <input type="hidden" name="value" class="hidden-value" value={JSON.stringify(coachingConfig)} />
      <label class="form-label">Sujet</label>
      <input type="text" class="form-input subject-input" placeholder="Sujet de l'email..." />
      <label class="form-label" style="margin-top:0.25rem">Corps</label>
      <textarea class="form-input cust-textarea body-input" rows={7} placeholder="Contenu de l'email..."></textarea>
      <div class="cust-hints">
        <p class="cust-hints-label">Variables disponibles — cliquez pour insérer :</p>
        <div class="cust-hints-list">
          {VARIABLE_HINTS['template_coaching_j14']!.map((v) => (
            <code class="cust-hint" data-var={v}>{v}</code>
          ))}
        </div>
      </div>
      <div class="sms-editor" style="display:none">
        <label class="form-label" style="margin-top:0.75rem">Corps du SMS</label>
        <textarea class="form-input sms-body-input" rows={3} maxlength={160} disabled
                  placeholder="Ex : Bonjour {{nom}}, votre formation {{nom_formation}} vous attend. Commencez ici : {{lien_acces}}"></textarea>
        <div class="sms-counter"><span class="sms-char-count">0</span>/160</div>
        <p class="sms-note">160 caractères max. Les variables sont remplacées par les vraies données.</p>
      </div>
      <div class="manual-actions">
        <button type="submit" class="btn btn-primary cust-save-btn">Sauvegarder</button>
        <button type="button" class="btn btn-secondary btn-improve">🔧 Améliorer avec l'IA</button>
      </div>
      <p class="ai-error improve-error"></p>
    </form>
    <div class="test-send-wrap">
      <button type="button" class="btn btn-secondary btn-test-send"
              data-config-type="template_coaching_j14" data-email={auth.email}>
        📧 M'envoyer un aperçu
      </button>
      <span class="test-send-feedback" aria-live="polite"></span>
    </div>
  </div>

  <div class="tab-panel hidden" data-panel="generate">
    <input type="text" class="form-input gen-formation" placeholder="Nom de votre formation" />
    <select class="form-input gen-tone">
      <option value="chaleureux">Chaleureux et bienveillant</option>
      <option value="professionnel">Professionnel et direct</option>
      <option value="motivant">Motivant et dynamique</option>
    </select>
    <input type="text" class="form-input gen-objective" placeholder="Objectif (ex : démarrer des élèves inactifs)" />
    <button class="btn btn-primary btn-generate" style="align-self:flex-start">✨ Générer l'email</button>
    <p class="ai-error gen-error"></p>
    <div class="generation-result hidden">
      <input type="text" class="form-input subject-preview" readonly placeholder="Sujet généré" />
      <textarea class="form-input body-preview" rows={6} readonly placeholder="Contenu généré"></textarea>
      <button class="btn btn-secondary btn-use-result" style="align-self:flex-start">Utiliser ce contenu →</button>
    </div>
  </div>
</div>
```

- [ ] **Step 4: Build check**

```bash
npm run build
```
Attendu : `0 errors`.

- [ ] **Step 5: Commit**

```bash
git add src/pages/client/customize.astro
git commit -m "feat(customize): add F14 predunning, F15 churn, F19 coaching panels"
```

---

## Task 4: Panels standalone — data attrs + toggles WA/SMS (checkout abandon + témoignages)

**Files:**
- Modify: `src/pages/client/customize.astro` (panels checkout_abandon, testimonial_j30, testimonial_j60)

Pour chaque panel standalone, ajouter : data attrs WA/SMS sur le `<div>`, toggles WA/SMS dans `.panel-right-actions`, bloc `.sms-editor` dans le formulaire manuel.

- [ ] **Step 1: Panel checkout_abandon — data attrs**

Trouver :
```astro
<div class="cust-card config-panel checkout-abandon-card"
     id="panel-template_checkout_abandon"
     data-config="template_checkout_abandon"
     data-subject={checkoutAbandonConfig.subject}
     data-body={checkoutAbandonConfig.body}
     data-label={checkoutAbandonConfig.label ?? 'Panier abandonné'}
     data-panel-active={checkoutAbandonConfig.active ? 'true' : 'false'}>
```
Remplacer par :
```astro
<div class="cust-card config-panel checkout-abandon-card"
     id="panel-template_checkout_abandon"
     data-config="template_checkout_abandon"
     data-subject={checkoutAbandonConfig.subject}
     data-body={checkoutAbandonConfig.body}
     data-label={checkoutAbandonConfig.label ?? 'Panier abandonné'}
     data-panel-active={checkoutAbandonConfig.active ? 'true' : 'false'}
     data-whatsapp-active={checkoutAbandonConfig.whatsapp_active ? 'true' : 'false'}
     data-sms-active={checkoutAbandonConfig.sms_active ? 'true' : 'false'}
     data-sms-body={checkoutAbandonConfig.sms_body ?? ''}>
```

- [ ] **Step 2: Panel checkout_abandon — toggles WA/SMS dans panel-right-actions**

Trouver dans ce panel :
```astro
<div class="panel-right-actions">
  <label class="toggle-switch" title="Activer/désactiver">
    <input type="checkbox" class="toggle-input" checked={checkoutAbandonConfig.active} />
    <span class="toggle-track"></span>
    <span class="toggle-label">{checkoutAbandonConfig.active ? 'Actif' : 'Inactif'}</span>
  </label>
</div>
```
Remplacer par :
```astro
<div class="panel-right-actions">
  <label class="toggle-switch" title="Activer/désactiver">
    <input type="checkbox" class="toggle-input" checked={checkoutAbandonConfig.active} />
    <span class="toggle-track"></span>
    <span class="toggle-label">{checkoutAbandonConfig.active ? 'Actif' : 'Inactif'}</span>
  </label>
  {whatsappConnected && (
    <label class="toggle-switch toggle-wa" title="Envoyer aussi par WhatsApp">
      <input type="checkbox" class="toggle-wa-input" checked={checkoutAbandonConfig.whatsapp_active} />
      <span class="toggle-track"></span>
      <span class="toggle-label-wa">WhatsApp</span>
    </label>
  )}
  <label class="toggle-switch toggle-sms" title="Envoyer aussi par SMS">
    <input type="checkbox" class="toggle-sms-input" checked={checkoutAbandonConfig.sms_active} />
    <span class="toggle-track"></span>
    <span class="toggle-label-sms">SMS</span>
  </label>
</div>
```

- [ ] **Step 3: Panel checkout_abandon — sms-editor dans le formulaire**

Dans ce panel, trouver (dans `<div class="tab-panel" data-panel="manual">`), après `</div>` fermant `.cust-hints` et avant `<div class="manual-actions">` :
```astro
<div class="sms-editor" style="display:none">
  <label class="form-label" style="margin-top:0.75rem">Corps du SMS</label>
  <textarea class="form-input sms-body-input" rows={3} maxlength={160} disabled
            placeholder="Ex : Bonjour {{nom}}, votre panier vous attend. Finalisez ici : {{lien_checkout}}"></textarea>
  <div class="sms-counter"><span class="sms-char-count">0</span>/160</div>
  <p class="sms-note">160 caractères max. Les variables sont remplacées par les vraies données.</p>
</div>
```

- [ ] **Step 4: Panel testimonial_j30 — même 3 modifications**

Pour le panel `template_testimonial_j30` (mêmes 3 opérations) :

Data attrs à ajouter sur le div :
```astro
data-whatsapp-active={testimonialJ30Config.whatsapp_active ? 'true' : 'false'}
data-sms-active={testimonialJ30Config.sms_active ? 'true' : 'false'}
data-sms-body={testimonialJ30Config.sms_body ?? ''}
```

Toggles WA/SMS dans `.panel-right-actions` après le toggle existant (même pattern que checkout_abandon avec `testimonialJ30Config`).

Bloc sms-editor dans le formulaire (après cust-hints, avant manual-actions) :
```astro
<div class="sms-editor" style="display:none">
  <label class="form-label" style="margin-top:0.75rem">Corps du SMS</label>
  <textarea class="form-input sms-body-input" rows={3} maxlength={160} disabled
            placeholder="Ex : Bonjour {{nom}}, un avis sur {{nom_formation}} ? Lien : {{lien_temoignage}}"></textarea>
  <div class="sms-counter"><span class="sms-char-count">0</span>/160</div>
  <p class="sms-note">160 caractères max. Les variables sont remplacées par les vraies données.</p>
</div>
```

- [ ] **Step 5: Panel testimonial_j60 — même 3 modifications**

Idem que testimonial_j30 mais avec `testimonialJ60Config` et `data-whatsapp-active={testimonialJ60Config.whatsapp_active ? 'true' : 'false'}` etc. Placeholder SMS identique.

- [ ] **Step 6: Build check**

```bash
npm run build
```
Attendu : `0 errors`.

- [ ] **Step 7: Commit**

```bash
git add src/pages/client/customize.astro
git commit -m "feat(customize): add WA/SMS toggles to standalone panels (checkout, testimonials)"
```

---

## Task 5: Enrichir initEditor() JS — WA/SMS + CSS (customize.astro)

**Files:**
- Modify: `src/pages/client/customize.astro` (bloc `<script>`, fonction `initEditor()`)

**Note portée des variables :** `smsBodyEl`, `smsEditor`, `smsCharCount`, `smsToggle`, `waToggleInput` sont déclarés AVANT le guard (comme `subjectInput`/`bodyInput` dans le pattern existant) et réutilisés dans les handlers post-guard. Ne pas les redéclarer après.

- [ ] **Step 1: Déclarer les variables SMS/WA et pré-charger avant le guard**

Dans `initEditor()`, ajouter JUSTE AVANT `if (card.dataset.initialized) return;` :
```js
const smsBodyEl    = card.querySelector<HTMLTextAreaElement>('.sms-body-input');
const smsEditor    = card.querySelector<HTMLElement>('.sms-editor');
const smsCharCount = card.querySelector<HTMLElement>('.sms-char-count');
const smsToggle    = card.querySelector<HTMLInputElement>('.toggle-sms-input');
const waToggleInput = card.querySelector<HTMLInputElement>('.toggle-wa-input');
// Pre-fill SMS on every page load (View Transitions)
if (smsBodyEl) {
  smsBodyEl.value = card.dataset.smsBody ?? '';
  if (smsCharCount) smsCharCount.textContent = String(smsBodyEl.value.length);
}
if (smsEditor && smsToggle) {
  smsEditor.style.display = smsToggle.checked ? '' : 'none';
  if (smsBodyEl) smsBodyEl.disabled = !smsToggle.checked;
}
```

- [ ] **Step 2: Mettre à jour le form submit handler pour inclure WA + SMS**

Trouver le `form.addEventListener('submit', ...)` dans `initEditor()` et remplacer par :
```js
const form = card.querySelector<HTMLFormElement>('form.cust-form');
if (form) {
  form.addEventListener('submit', () => {
    const hiddenVal = form.querySelector<HTMLInputElement>('.hidden-value');
    if (!hiddenVal) return;
    const subject     = subjectInput?.value ?? '';
    const body        = bodyInput?.value    ?? '';
    const customLabel = card.dataset.label  ?? '';
    const isActive    = card.querySelector<HTMLInputElement>('.toggle-input')?.checked ?? true;
    const obj: Record<string, string | boolean> = { subject, body, active: isActive };
    if (customLabel)    obj.label           = customLabel;
    if (waToggleInput)  obj.whatsapp_active  = waToggleInput.checked;
    if (smsToggle)      obj.sms_active       = smsToggle.checked;
    if (smsBodyEl)      obj.sms_body         = smsBodyEl.value;
    hiddenVal.value = JSON.stringify(obj);
  });
}
```

- [ ] **Step 3: Mettre à jour le toggle principal pour inclure WA + SMS dans le JSON persisté**

Trouver le `toggleInput.addEventListener('change', async () => {` existant. À l'intérieur, remplacer uniquement la ligne de construction de `value` :
```js
// Ancienne ligne :
const value = JSON.stringify({ subject, body, active: isActive, ...(label ? { label } : {}) });
// Nouvelle ligne (remplace) :
const value = JSON.stringify({
  subject, body, active: isActive,
  ...(label         ? { label }                              : {}),
  ...(waToggleInput ? { whatsapp_active: waToggleInput.checked } : {}),
  ...(smsToggle     ? { sms_active: smsToggle.checked }         : {}),
  ...(smsBodyEl     ? { sms_body: smsBodyEl.value }             : {}),
});
```

- [ ] **Step 4: Ajouter le handler toggle WhatsApp (après le guard, après le handler toggle principal)**

Insérer après la fermeture `}` du bloc `if (toggleInput) { ... }` :
```js
if (waToggleInput) {
  waToggleInput.addEventListener('change', async () => {
    const isWa      = waToggleInput.checked;
    const configKey = card.dataset.config ?? '';
    const subject   = subjectInput?.value ?? card.dataset.subject ?? '';
    const body      = bodyInput?.value    ?? card.dataset.body    ?? '';
    const label     = card.dataset.label  ?? '';
    const isActive  = card.querySelector<HTMLInputElement>('.toggle-input')?.checked ?? true;
    const value = JSON.stringify({
      subject, body, active: isActive,
      whatsapp_active: isWa,
      sms_active: smsToggle?.checked ?? false,
      sms_body: smsBodyEl?.value ?? '',
      ...(label ? { label } : {}),
    });
    try {
      const res = await fetch('/api/config-update', {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ config_type: configKey, value }),
      });
      if (!res.ok) throw new Error();
    } catch {
      waToggleInput.checked = !isWa;
    }
  });
}
```

- [ ] **Step 5: Ajouter le handler toggle SMS (après le handler WA)**

```js
if (smsToggle && smsEditor) {
  smsToggle.addEventListener('change', async () => {
    const isSms     = smsToggle.checked;
    smsEditor.style.display = isSms ? '' : 'none';
    if (smsBodyEl) smsBodyEl.disabled = !isSms;
    const configKey = card.dataset.config ?? '';
    const subject   = subjectInput?.value ?? card.dataset.subject ?? '';
    const body      = bodyInput?.value    ?? card.dataset.body    ?? '';
    const label     = card.dataset.label  ?? '';
    const isActive  = card.querySelector<HTMLInputElement>('.toggle-input')?.checked ?? true;
    const value = JSON.stringify({
      subject, body, active: isActive,
      whatsapp_active: waToggleInput?.checked ?? false,
      sms_active: isSms,
      sms_body: smsBodyEl?.value ?? '',
      ...(label ? { label } : {}),
    });
    try {
      await fetch('/api/config-update', {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ config_type: configKey, value }),
      });
    } catch { /* non-fatal */ }
  });
}
```

- [ ] **Step 6: Ajouter le compteur SMS sur input**

Après les handlers des toggles, ajouter :
```js
if (smsBodyEl && smsCharCount) {
  smsBodyEl.addEventListener('input', () => {
    smsCharCount.textContent = String(smsBodyEl.value.length);
  });
}
```

- [ ] **Step 7: Ajouter le CSS pour les nouveaux éléments**

Dans le bloc `<style is:global>` ou `<style>` de la page (en fin de fichier, avant `</ClientLayout>`), ajouter :
```css
.sms-counter { text-align: right; font-size: 0.75rem; color: var(--gray); margin-top: 0.25rem; }
.sms-note { font-size: 0.8125rem; color: var(--gray); margin: 0.25rem 0 0.5rem; }
.toggle-wa .toggle-track,
.toggle-sms .toggle-track { background: rgba(46,139,240,0.15); }
.toggle-wa input:checked ~ .toggle-track,
.toggle-sms input:checked ~ .toggle-track { background: rgba(46,139,240,0.35); }
```

- [ ] **Step 8: Build check**

```bash
npm run build
```
Attendu : `0 errors`.

- [ ] **Step 9: Commit**

```bash
git add src/pages/client/customize.astro
git commit -m "feat(customize): enrich initEditor() for WA/SMS toggles and SMS counter"
```

---

## Task 6: Créer la route proxy whatsapp-connect.ts

**Files:**
- Create: `src/pages/api/whatsapp-connect.ts`

- [ ] **Step 1: Créer le fichier**

```ts
export const prerender = false;
import type { APIRoute } from 'astro';
import { getClientFromCookie } from '../../lib/auth';
import { jsonRes } from '../../lib/api';

export const POST: APIRoute = async ({ request }) => {
  const auth = await getClientFromCookie(request);
  if (!auth) return jsonRes({ error: 'Non autorisé' }, 401);

  let body: { phone_number_id?: string; access_token?: string };
  try {
    body = await request.json();
  } catch {
    return jsonRes({ error: 'Corps JSON invalide' }, 400);
  }

  const { phone_number_id, access_token } = body;
  if (!phone_number_id || !access_token) {
    return jsonRes({ error: 'Phone Number ID et Access Token requis' }, 400);
  }

  try {
    const res = await fetch(`${import.meta.env.AEVUM_URL}/client/whatsapp/connect`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        Authorization: `Bearer ${auth.token}`,
      },
      body: JSON.stringify({ phone_number_id, access_token }),
      signal: AbortSignal.timeout(8000),
    });
    const data = await res.json().catch(() => ({}));
    return jsonRes(data, res.status);
  } catch {
    return jsonRes({ error: 'Erreur réseau' }, 500);
  }
};
```

- [ ] **Step 2: Build check**

```bash
npm run build
```
Attendu : `0 errors`.

- [ ] **Step 3: Commit**

```bash
git add src/pages/api/whatsapp-connect.ts
git commit -m "feat(api): add whatsapp-connect proxy route (F16)"
```

---

## Task 7: Créer la route proxy whatsapp-disconnect.ts

**Files:**
- Create: `src/pages/api/whatsapp-disconnect.ts`

- [ ] **Step 1: Créer le fichier**

```ts
export const prerender = false;
import type { APIRoute } from 'astro';
import { getClientFromCookie } from '../../lib/auth';
import { jsonRes } from '../../lib/api';

export const POST: APIRoute = async ({ request }) => {
  const auth = await getClientFromCookie(request);
  if (!auth) return jsonRes({ error: 'Non autorisé' }, 401);

  try {
    const res = await fetch(`${import.meta.env.AEVUM_URL}/client/whatsapp/connect`, {
      method: 'DELETE',
      headers: { Authorization: `Bearer ${auth.token}` },
      signal: AbortSignal.timeout(8000),
    });
    const data = await res.json().catch(() => ({}));
    return jsonRes(data, res.status);
  } catch {
    return jsonRes({ error: 'Erreur réseau' }, 500);
  }
};
```

- [ ] **Step 2: Build check + commit**

```bash
npm run build
git add src/pages/api/whatsapp-disconnect.ts
git commit -m "feat(api): add whatsapp-disconnect proxy route (F16)"
```

---

## Task 8: Mettre à jour settings.astro — frontmatter + HTML + JS

**Files:**
- Modify: `src/pages/client/settings.astro`

### Frontmatter

- [ ] **Step 1: Ajouter les variables de feedback WA et remplacer le fetch /client/me**

En haut du frontmatter (après les autres variables `let`), ajouter :
```ts
let whatsappMsg = '';
let whatsappError = '';
```

Remplacer le bloc `let meData = null; try { ... } catch {}` existant par un `Promise.allSettled` :
```ts
const [meResult, configsResult] = await Promise.allSettled([
  fetch(`${import.meta.env.AEVUM_URL}/client/me`, {
    headers: { Authorization: `Bearer ${auth.token}` },
    signal: AbortSignal.timeout(8000),
  }),
  fetch(`${import.meta.env.AEVUM_URL}/client/configs`, {
    headers: { Authorization: `Bearer ${auth.token}` },
    signal: AbortSignal.timeout(8000),
  }),
]);

let meData: { paused_until?: string | null; whatsapp_connected?: boolean } | null = null;
if (meResult.status === 'fulfilled') {
  if (meResult.value.status === 401) {
    Astro.cookies.delete('aevum_token', { path: '/' });
    return Astro.redirect('/login');
  }
  if (meResult.value.ok) meData = await meResult.value.json().catch(() => null);
}

let configs: Record<string, string> = {};
if (configsResult.status === 'fulfilled' && configsResult.value.ok) {
  const arr: { config_type: string; value: string }[] = await configsResult.value.json().catch(() => []);
  if (Array.isArray(arr)) {
    for (const c of arr) configs[c.config_type] = c.value;
  }
}

const pausedUntil: string | null = meData?.paused_until ?? null;
const whatsappConnected: boolean = meData?.whatsapp_connected ?? false;
const rapportVideoActive: boolean = configs['rapport_video_active'] === 'true';
```

- [ ] **Step 2: Ajouter les handlers POST pour WA (dans le bloc `if (Astro.request.method === 'POST')`)**

Après le handler `pause_disable`, ajouter :
```ts
if (action === 'whatsapp_connect') {
  const phoneNumberId = (form.get('phone_number_id') as string ?? '').trim();
  const accessToken   = (form.get('access_token') as string ?? '').trim();
  if (!phoneNumberId || !accessToken) {
    whatsappError = 'Veuillez renseigner les deux champs.';
  } else {
    try {
      const res = await fetch(`${import.meta.env.AEVUM_URL}/client/whatsapp/connect`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${auth.token}` },
        body: JSON.stringify({ phone_number_id: phoneNumberId, access_token: accessToken }),
        signal: AbortSignal.timeout(8000),
      });
      if (res.ok) return Astro.redirect('/client/settings');
      const d = await res.json().catch(() => ({}));
      whatsappError = (d as Record<string, string>).error ?? (d as Record<string, string>).message ?? 'Impossible de connecter WhatsApp.';
    } catch {
      whatsappError = 'Erreur réseau. Réessayez.';
    }
  }
}

if (action === 'whatsapp_disconnect') {
  try {
    const res = await fetch(`${import.meta.env.AEVUM_URL}/client/whatsapp/connect`, {
      method: 'DELETE',
      headers: { Authorization: `Bearer ${auth.token}` },
      signal: AbortSignal.timeout(8000),
    });
    if (res.ok) return Astro.redirect('/client/settings');
    const d = await res.json().catch(() => ({}));
    whatsappError = (d as Record<string, string>).error ?? (d as Record<string, string>).message ?? 'Impossible de déconnecter WhatsApp.';
  } catch {
    whatsappError = 'Erreur réseau. Réessayez.';
  }
}
```

### HTML — nouvelles sections

- [ ] **Step 3: Insérer la section WhatsApp (après "Mode pause", avant "Deliverability")**

Trouver le div de la section "Deliverability" et insérer avant lui :
```astro
<!-- F16 — WhatsApp Business -->
<div class="card settings-card">
  <h2 class="settings-card-title">WhatsApp Business</h2>
  {whatsappMsg && <div class="success-msg" role="alert"><span>{whatsappMsg}</span></div>}
  {whatsappError && <div class="settings-error" role="alert">{whatsappError}</div>}
  {whatsappConnected ? (
    <div>
      <p style="margin:0 0 1rem">
        <span class="wa-connected-badge">● WhatsApp connecté</span>
      </p>
      <form method="POST" style="margin:0">
        <input type="hidden" name="action" value="whatsapp_disconnect" />
        <button type="submit" class="btn btn-secondary">Déconnecter</button>
      </form>
    </div>
  ) : (
    <div style="display:flex;flex-direction:column;gap:1.25rem">
      <p style="color:var(--gray-light);font-size:0.9375rem;margin:0">
        Connectez votre compte WhatsApp Business pour envoyer vos messages aussi via WhatsApp.
      </p>
      <form method="POST" style="display:flex;flex-direction:column;gap:1.25rem">
        <input type="hidden" name="action" value="whatsapp_connect" />
        <div class="form-group">
          <label for="wa-phone-id" class="form-label">Phone Number ID</label>
          <input type="text" id="wa-phone-id" name="phone_number_id" class="form-input"
                 placeholder="Ex : 123456789012345" />
        </div>
        <div class="form-group">
          <label for="wa-token" class="form-label">Access Token</label>
          <input type="text" id="wa-token" name="access_token" class="form-input"
                 placeholder="Votre token d'accès Meta" />
        </div>
        <a href="https://developers.facebook.com/docs/whatsapp/cloud-api/get-started"
           target="_blank" rel="noopener noreferrer"
           style="font-size:0.875rem;color:var(--gray-light)">
          Obtenir ces identifiants sur Meta for Developers →
        </a>
        <button type="submit" class="btn btn-primary" style="align-self:flex-start">Connecter</button>
      </form>
    </div>
  )}
</div>

<!-- F20 — SMS (informatif) -->
<div class="card settings-card">
  <h2 class="settings-card-title">SMS</h2>
  <p style="color:var(--gray-light);font-size:0.9375rem;margin:0">
    Les SMS sont envoyés depuis le numéro AEVUM. Assurez-vous que vos élèves ont renseigné leur numéro de téléphone lors de l'achat sur Stripe.
  </p>
</div>

<!-- F17 — Rapport vidéo hebdomadaire -->
<div class="card settings-card">
  <h2 class="settings-card-title">Rapport vidéo hebdomadaire</h2>
  <label class="toggle-switch" style="margin-bottom:1rem">
    <input type="checkbox" id="rapport-toggle-input" checked={rapportVideoActive} />
    <span class="toggle-track"></span>
    <span class="toggle-label" id="rapport-toggle-label">{rapportVideoActive ? 'Actif' : 'Inactif'}</span>
  </label>
  <p style="color:var(--gray-light);font-size:0.9375rem;margin:0 0 0.5rem">
    Chaque lundi à 8h, vous recevez par email une vidéo de 60-90 secondes résumant votre semaine : nouveaux élèves, impayés récupérés, emails envoyés.
  </p>
  <p style="color:var(--gray);font-size:0.875rem;margin:0">
    Voix off française générée par IA + slides automatiques.
  </p>
  <p class="rapport-error" style="display:none;color:#EF4444;font-size:0.875rem;margin-top:0.5rem"></p>
</div>
```

- [ ] **Step 4: Ajouter le CSS pour wa-connected-badge**

Dans le bloc `<style>` en fin de fichier, ajouter :
```css
.wa-connected-badge { color: #10B981; font-weight: 500; }
```

### JS — rapport vidéo toggle

- [ ] **Step 5: Ajouter le script pour le toggle rapport vidéo**

Ajouter avant `</ClientLayout>` :
```astro
<script>
  function initRapportToggle() {
    const toggle   = document.getElementById('rapport-toggle-input') as HTMLInputElement | null;
    const label    = document.getElementById('rapport-toggle-label');
    const errorEl  = document.querySelector<HTMLElement>('.rapport-error');
    if (!toggle || toggle.dataset.initialized) return;
    toggle.dataset.initialized = 'true';
    toggle.addEventListener('change', async () => {
      const active = toggle.checked;
      if (label) label.textContent = active ? 'Actif' : 'Inactif';
      if (errorEl) errorEl.style.display = 'none';
      try {
        const res = await fetch('/api/config-update', {
          method: 'PUT',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ config_type: 'rapport_video_active', value: active ? 'true' : 'false' }),
        });
        if (!res.ok) throw new Error();
      } catch {
        toggle.checked = !active;
        if (label) label.textContent = !active ? 'Actif' : 'Inactif';
        if (errorEl) { errorEl.textContent = 'Erreur lors de la sauvegarde.'; errorEl.style.display = ''; }
      }
    });
  }
  initRapportToggle();
  document.addEventListener('astro:page-load', initRapportToggle);
</script>
```

- [ ] **Step 6: Build check**

```bash
npm run build
```
Attendu : `0 errors`.

- [ ] **Step 7: Commit**

```bash
git add src/pages/client/settings.astro
git commit -m "feat(settings): add WhatsApp (F16), SMS info (F20), rapport vidéo (F17) sections"
```

---

## Task 9: Badge churn dans drawer students.astro (F15)

**Files:**
- Modify: `src/pages/client/students.astro`

- [ ] **Step 1: Ajouter le badge dans le rendu du drawer**

Dans le script JS, trouver le template string de `drawerBody.innerHTML` :
```js
<div class="drawer-meta">
  <span class="stu-badge stu-badge--${detail.status}">${statusLabel}</span>
  <span class="drawer-date">Inscrit le ${...}</span>
</div>
```
Remplacer par :
```js
<div class="drawer-meta">
  <span class="stu-badge stu-badge--${detail.status}">${statusLabel}</span>
  ${detail.churn_risk ? '<span class="stu-badge stu-badge--churn">À risque</span>' : ''}
  <span class="drawer-date">Inscrit le ${detail.created_at ? new Date(detail.created_at).toLocaleDateString('fr-FR', { dateStyle: 'short' }) : '—'}</span>
</div>
```

- [ ] **Step 2: Ajouter le CSS pour le badge churn**

Dans le bloc `<style>` en fin de fichier, après `.stu-badge--blackliste { ... }`, ajouter :
```css
.stu-badge--churn { background: rgba(249,115,22,0.15); color: #F97316; }
```

- [ ] **Step 3: Build check + commit**

```bash
npm run build
git add src/pages/client/students.astro
git commit -m "feat(students): add churn risk badge in drawer (F15)"
```

---

## Task 10: Lien vidéo dans history.astro (F17)

**Files:**
- Modify: `src/pages/client/history.astro`

- [ ] **Step 1: Modifier la colonne Détails pour les entrées rapport_video_sent**

Trouver :
```astro
<td class="td-details">{entry.details ?? '—'}</td>
```
Remplacer par :
```astro
<td class="td-details">
  {entry.action === 'rapport_video_sent' && entry.details
    ? <a href={entry.details} target="_blank" rel="noopener noreferrer" class="hist-video-link">Voir la vidéo →</a>
    : (entry.details ?? '—')}
</td>
```

- [ ] **Step 2: Ajouter le CSS**

Dans le bloc `<style>`, ajouter :
```css
.hist-video-link { color: var(--light); text-decoration: underline; font-weight: 500; }
.hist-video-link:hover { color: #2E8BF0; }
```

- [ ] **Step 3: Build check + commit**

```bash
npm run build
git add src/pages/client/history.astro
git commit -m "feat(history): link to video for rapport_video_sent entries (F17)"
```

---

## Task 11: Build final + vérifications

**Files:** aucun changement de code

- [ ] **Step 1: Build propre**

```bash
npm run build
```
Attendu : `0 TypeScript errors`, build Vercel sans warning critique.

- [ ] **Step 2: Vérifier la checklist spec**

- [ ] Les toggles WhatsApp dans customize n'apparaissent pas si `whatsapp_connected = false` (tester avec `whatsappConnected = false` dans le frontmatter temporairement)
- [ ] Le formulaire WhatsApp dans settings : visible si non connecté, badge si connecté
- [ ] Le compteur SMS se met à jour en temps réel sur chaque panel
- [ ] Le textarea SMS est disabled quand le toggle SMS est décoché
- [ ] Le badge "À risque" n'apparaît que si `detail.churn_risk === true`
- [ ] L'entrée `rapport_video_sent` dans l'historique affiche un lien cliquable
- [ ] Guard `dataset.initialized` présent sur le toggle rapport vidéo (settings.astro Step 5)
- [ ] `npm run build` : 0 erreurs TypeScript

- [ ] **Step 3: Push**

```bash
git push origin feat/phase2-features8-12
```
